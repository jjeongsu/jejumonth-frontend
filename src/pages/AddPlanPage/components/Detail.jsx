import React from 'react';

const Detail = ({ onNext, contentId }) => {
  // 컨텐츠 id로 데이터 가져오기
  return (
    <div>
      Detail
      <button>일정 생성</button>
    </div>
  );
};

export default Detail;
